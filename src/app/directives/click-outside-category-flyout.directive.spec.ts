import { ClickOutsideCategoryFlyoutDirective } from './click-outside-category-flyout.directive';

describe('ClickOutsideCategoryFlyoutDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickOutsideCategoryFlyoutDirective();
    expect(directive).toBeTruthy();
  });
});
